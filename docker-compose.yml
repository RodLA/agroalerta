version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MONGO_URI=mongodb://your-mongo-host:27017/your-db
      - CORS_ORIGIN=http://your-frontend-domain.com
